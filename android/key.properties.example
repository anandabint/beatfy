## Copy this file to `android/key.properties` (same folder) and fill in your
## own values after generating a release keystore — see README.md "Release
## build" section. `key.properties` itself is git-ignored and must NEVER be
## committed.
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=beatfy
storeFile=../beatfy-release.jks
